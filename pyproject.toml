[project]
name = "cir-arc"
version = "0.1.0"
description = "ARC-AGI Phase 1 — Curriculum-Informed Reasoning"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-v"

[tool.ruff]
line-length = 100
target-version = "py311"
